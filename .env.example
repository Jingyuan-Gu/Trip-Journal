# Server-only settings. Copy to .env.local, then restart the dev server.
AI_MODE=mock
AI_BASE_URL=https://api.openai.com/v1
AI_MODEL=
AI_API_KEY=
# Optional HTTPS JSON reverse-geocoding adapter (latitude/longitude -> placeName/city/district/confidence).
GEOCODING_PROVIDER_URL=
GEOCODING_API_KEY=
